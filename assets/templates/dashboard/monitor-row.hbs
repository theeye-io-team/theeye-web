<div class="itemRow resource-container panel panel-default js-searchable-item" data-tags="{{ attributes.formatted_tags }}">
  <div class="panel-heading" role="tab">
    <h4 class="panel-title {{ attributes.type }}">
      <span class="collapsed"
        data-toggle="collapse"
        data-parent="#monitor-accordion"
        href="#collapse{{ attributes.id }}"
        aria-expanded="false"
        aria-controls="collapse{{ attributes.id }}">
        <div class="panel-title-content">
          <span class="panel-item name">
            {{ attributes.description }}
            <small> > {{ attributes.type }}</small>
          </span>
          <span class="panel-item icons">
            <button data-search="{{ attributes.description }}"
              class="btn btn-primary resource-search">
              <span class="glyphicon glyphicon-search"></span>
            </button>
            <a title="Workflow"
              data-hook="workflow-button"
              href="/admin/workflow?node={{ attributes.monitor.id }}"
              class="tooltiped btn-icon-image flowchart-icon">
            </a>
            <button class="monitorStats btn btn-icon-image icon-image-stats tooltiped" title="Stats"> </button>
            <button class="editMonitors btn btn-icon-image icon-image-edit tooltiped" title="Edit monitors"> </button>
            <div class="state-resource-container">
              <span class="tooltiped state-icon "
                title="{{ attributes.state }}">
              </span>
            </div>
          </span>
        </div>
      </span>
    </h4>
  </div>
  <!-- COLLAPSE -->
  <div id="collapse{{ attributes.id }}"
    class="panel-collapse collapse"
    role="tabpanel"
    aria-labelledby="heading{{ attributes.id }}">
    <div class="panel-body">
      <div>
        <h4>Host: {{ attributes.hostname }}</h4>
        <table class="table table-stripped">
          <thead>
            <tr>
              <th></th>
              <th>Type</th>
              <th>Status</th>
              <th>Last updated</th>
            </tr>
          </thead>
          <tbody>
            <tr>
              <td class="text-center">
                <span class="state-icon "></span>
              </td>
              <td>{{ attributes.type }}</td>
              <td><span class="state_text">{{ attributes.state }}</span></td>
              <td><span class="state_last_update">
                  {{ attributes.last_update_formated }}
              </span></td>
            </tr>
            <!--
            <tr class="resource<%=subresource.id%>">
              <td class="text-center">
                <span class="state-icon <%=iconsDicc[subresource.state]%>"></span>
              </td>
              <td><%= subresource.type %></td>
              <td><span class="state_text"><%= subresource.state %></span></td>
              <td><span class="state_last_update"><%= moment(subresource.last_update).startOf('second').fromNow() %></span></td>
            </tr>
            -->
          </tbody>
        </table>
      </div>
    </div>
  </div>
</div>
